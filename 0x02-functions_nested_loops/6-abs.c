#include "main.h"

/**
 * _abs - entry point
 * @value: function parameter
 * Return: 0 (success)
 */

int _abs(int n)
{
	int cal[1];

	if (n < 0)
		cal[0] = n * -1;
	else if (value >= 0)
		cal[0] = n;
	_putchar(cal[0]);
	return (0);
}
